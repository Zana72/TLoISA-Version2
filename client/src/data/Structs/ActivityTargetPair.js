
export default function activityTargetPair(id, priority, targetGroup, activity) {
    return {
        id: id,
        priority: priority,
        targetGroup: targetGroup,
        activity: activity
    }

}